/**
 * Inputs v2 — public surface (Interface Guide v2, station S2).
 * The rebuilt Inputs body; ships alongside the v1 InputsWorkspace until
 * the read-path (source + mapping + scoring) is fully wired.
 */
export { InputsPanelV2, policyHeadlinePremium } from "./InputsPanelV2";
export type {
  InputsPanelV2Props,
  InputStageLike,
} from "./InputsPanelV2";
