{
  "name": "@openrater/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Pure types, schemas, and runtime for the OpenRater Plan Format. Owns the Block contract, Plan JSON, the runtime (compile + run), the kind registry, the type system. No React, no I/O, no insurance vocabulary. Importable from any context. See docs/specs/plan-format-v1.md for the normative spec.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": ["src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
