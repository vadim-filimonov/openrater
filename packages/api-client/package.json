{
  "name": "@openrater/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Typed REST SDK for the OpenRater API Lab backend. One function per endpoint; handles error normalization + Zod-validated responses. The single source of truth for how frontends talk to api-lab/backend — apps MUST go through this, never call fetch() directly. Wraps TanStack Query in @openrater/hooks.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": ["src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@openrater/contracts": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
